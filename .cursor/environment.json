{
  "name": "test-repo",
  "install": "npm ci",
  "ports": [
    { "name": "client", "port": 5173 },
    { "name": "api", "port": 3001 }
  ],
  "terminals": [
    {
      "name": "server",
      "command": "npm run dev:server",
      "description": "Express Todo API (watch mode) on http://localhost:3001"
    },
    {
      "name": "client",
      "command": "npm run dev:client",
      "description": "Vite dev server on http://localhost:5173 (proxies /api to :3001)"
    }
  ]
}
